---
plugin: amazon.aws.aws_ec2

regions:
  - us-east-2

hostnames:
  - ip-address

include_filters:
  tag:Environment: prod
  tag:Project: final-project

exclude_filters:
  - "tag:Role": database
  - "tag:Role": bastion

leading_separator: true
keyed_groups:
  - key: tags.Role
