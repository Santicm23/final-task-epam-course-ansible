---
plugin: amazon.aws.aws_ec2

regions:
  - us-east-2

hostnames:
  - ip-address

include_filters:
  tag:Environment: staging
  tag:Project: final-project

exclude_filters:
  tag:Role:
    - database
    - bastion

leading_separator: false
keyed_groups:
  - key: tags.Role
