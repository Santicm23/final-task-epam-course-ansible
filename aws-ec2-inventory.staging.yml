---
plugin: amazon.aws.aws_ec2

regions:
  - us-east-2

hostnames:
  - ip-address

filters:
  - tag:Environment: staging
  - tag:Project: final-project
  - tag:Role:
      - frontend
      - backend

leading_separator: false
keyed_groups:
  - key: tags.Role
